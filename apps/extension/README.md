# Resumora Job Capture

Manifest V3 browser extension for passing visible job-page text to Resumora through a URL fragment. The fragment stays client-side, is removed after import, and the extension requests access only to the active tab.

For local development, open `chrome://extensions`, enable Developer mode, choose **Load unpacked**, and select this directory. Set the workspace address from the extension popup before using a deployed app.
